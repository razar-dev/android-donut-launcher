package ussr.razar.android.content

object LauncherMetadata {
    /*
	 * Contains the current "Android AppWidget Extension API Version" 
	 */
    const val CurrentAPIVersion: Int = 2

    object Requirements {

        /*
		 * Metadata name for the required API Version
		 */
        const val APIVersion: String = "LauncherMetadata.Requirements.APIVersion"

    }
}